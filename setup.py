from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A repo for holding the data, replication code, and checking code for published discrete choice papers.',
    author='Timothy Brathwaite',
    license='MIT',
)
